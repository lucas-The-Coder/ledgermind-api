services:
  - type: web
    name: ledgermind-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: OPENAI_KEY
        sync: false
      - key: APP_SECRET
        sync: false
